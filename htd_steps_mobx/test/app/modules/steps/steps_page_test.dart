import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_steps_mobx/app/modules/steps/steps_page.dart';

void main() {
  testWidgets('StepsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StepsPage(title: 'Steps')));
    //  final titleFinder = find.text('Steps');
    //  expect(titleFinder, findsOneWidget);
  });
}
