import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_steps_mobx/app/modules/home/steps/step/step_page.dart';

void main() {
  testWidgets('StepPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(StepPage(title: 'Step')));
    //  final titleFinder = find.text('Step');
    //  expect(titleFinder, findsOneWidget);
  });
}
