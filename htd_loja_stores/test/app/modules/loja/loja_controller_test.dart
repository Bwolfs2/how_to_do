import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:htd_loja_store/app/modules/loja/loja_controller.dart';
import 'package:htd_loja_store/app/modules/loja/loja_module.dart';

void main() {
  initModule(LojaModule());
  // LojaController loja;
  //
  setUp(() {
    //     loja = LojaModule.to.get<LojaController>();
  });

  group('LojaController Test', () {
    //   test("First Test", () {
    //     expect(loja, isInstanceOf<LojaController>());
    //   });

    //   test("Set Value", () {
    //     expect(loja.value, equals(0));
    //     loja.increment();
    //     expect(loja.value, equals(1));
    //   });
  });
}
