import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('LojaPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(LojaPage(title: 'Loja')));
    //  final titleFinder = find.text('Loja');
    //  expect(titleFinder, findsOneWidget);
  });
}
