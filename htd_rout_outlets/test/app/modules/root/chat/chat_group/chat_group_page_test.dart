import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('ChatGroupPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ChatGroupPage(title: 'ChatGroup')));
    //  final titleFinder = find.text('ChatGroup');
    //  expect(titleFinder, findsOneWidget);
  });
}
