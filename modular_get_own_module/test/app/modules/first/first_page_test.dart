import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:modular_get_own_module/app/modules/first/first_page.dart';

void main() {
  testWidgets('FirstPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(FirstPage(title: 'First')));
    //  final titleFinder = find.text('First');
    //  expect(titleFinder, findsOneWidget);
  });
}
