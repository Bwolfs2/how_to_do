import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_will_pop_scope/app/modules/new/new_page.dart';

main() {
  testWidgets('NewPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewPage(title: 'New')));
    //  final titleFinder = find.text('New');
    //  expect(titleFinder, findsOneWidget);
  });
}
