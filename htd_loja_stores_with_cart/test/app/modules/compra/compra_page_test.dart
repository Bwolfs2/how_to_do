import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_loja_store/app/modules/compra/compra_page.dart';

main() {
  testWidgets('CompraPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CompraPage(title: 'Compra')));
    //  final titleFinder = find.text('Compra');
    //  expect(titleFinder, findsOneWidget);
  });
}
