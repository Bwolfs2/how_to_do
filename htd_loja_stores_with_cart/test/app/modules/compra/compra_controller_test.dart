import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:htd_loja_store/app/modules/compra/compra_controller.dart';
import 'package:htd_loja_store/app/modules/compra/compra_module.dart';

void main() {
  initModule(CompraModule());
  // CompraController compra;
  //
  setUp(() {
    //     compra = CompraModule.to.get<CompraController>();
  });

  group('CompraController Test', () {
    //   test("First Test", () {
    //     expect(compra, isInstanceOf<CompraController>());
    //   });

    //   test("Set Value", () {
    //     expect(compra.value, equals(0));
    //     compra.increment();
    //     expect(compra.value, equals(1));
    //   });
  });
}
