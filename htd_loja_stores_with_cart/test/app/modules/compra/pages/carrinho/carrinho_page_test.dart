import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_loja_store/app/modules/compra/pages/carrinho/carrinho_page.dart';

main() {
  testWidgets('CarrinhoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CarrinhoPage(title: 'Carrinho')));
    //  final titleFinder = find.text('Carrinho');
    //  expect(titleFinder, findsOneWidget);
  });
}
