import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_loja_store/app/modules/pagamento/pagamento_page.dart';

main() {
  testWidgets('PagamentoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PagamentoPage(title: 'Pagamento')));
    //  final titleFinder = find.text('Pagamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
