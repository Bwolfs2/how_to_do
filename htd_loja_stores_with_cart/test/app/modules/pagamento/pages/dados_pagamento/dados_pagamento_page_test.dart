import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_loja_store/app/modules/pagamento/pages/dados_pagamento/dados_pagamento_page.dart';

main() {
  testWidgets('DadosPagamentoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(DadosPagamentoPage(title: 'DadosPagamento')));
    //  final titleFinder = find.text('DadosPagamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
