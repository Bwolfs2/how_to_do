import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_loja_store/app/modules/exemplo/exemplo_page.dart';

main() {
  testWidgets('ExemploPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ExemploPage(title: 'Exemplo')));
    //  final titleFinder = find.text('Exemplo');
    //  expect(titleFinder, findsOneWidget);
  });
}
