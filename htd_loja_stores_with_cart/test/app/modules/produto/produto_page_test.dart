import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_loja_store/app/modules/produto/produto_page.dart';

main() {
  testWidgets('ProdutoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(ProdutoPage(title: 'Produto')));
    //  final titleFinder = find.text('Produto');
    //  expect(titleFinder, findsOneWidget);
  });
}
