import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_modular_guards/app/modules/admin/admin_page.dart';

void main() {
  testWidgets('AdminPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AdminPage(title: 'Admin')));
    //  final titleFinder = find.text('Admin');
    //  expect(titleFinder, findsOneWidget);
  });
}
