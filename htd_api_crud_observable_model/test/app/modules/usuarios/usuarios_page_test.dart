import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:htd_api_crud_observable_model/app/modules/usuarios/usuarios_page.dart';

main() {
  testWidgets('UsuariosPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(UsuariosPage(title: 'Usuarios')));
    //  final titleFinder = find.text('Usuarios');
    //  expect(titleFinder, findsOneWidget);
  });
}
