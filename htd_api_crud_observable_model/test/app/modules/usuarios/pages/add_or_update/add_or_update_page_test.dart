import 'package:flutter_test/flutter_test.dart';

main() {
  testWidgets('AddOrUpdatePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddOrUpdatePage(title: 'AddOrUpdate')));
    //  final titleFinder = find.text('AddOrUpdate');
    //  expect(titleFinder, findsOneWidget);
  });
}
